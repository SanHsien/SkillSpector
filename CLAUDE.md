> **Fork notice (SanHsien maintenance line):** this is a Windows-first maintenance fork of
> [`NVIDIA/SkillSpector`](https://github.com/NVIDIA/SkillSpector). Upstream ships neither a
> `CLAUDE.md` nor an `AGENTS.md`, so unlike a patched upstream file, this one is entirely
> fork-owned. The single source of truth for AI agents here is [`AGENTS.md`](AGENTS.md)
> (Traditional Chinese); read it first — it covers the project layout, upstream design
> conventions, and this fork's maintenance rules in one place. Fork boundary: never push,
> open a PR, or release against the upstream repo — only `origin`
> (`SanHsien/SkillSpector`), unless the maintainer explicitly agrees to contribute
> upstream in the current conversation. Reply to the maintainer in Traditional Chinese.

This file intentionally stays a pointer rather than duplicating `AGENTS.md`, so the two
never drift out of sync — update `AGENTS.md` when the rules change.
