---
description: PR、push、release 一律指向 SanHsien/SkillSpector；回貢上游需要當次對話明確同意
alwaysApply: true
---

# 對外只打本 fork

本 repo fork 自 [`NVIDIA/SkillSpector`](https://github.com/NVIDIA/SkillSpector)（Apache License 2.0）。
PR、push、release 一律指向 `SanHsien/SkillSpector`。

**根因是機制不是粗心：** `gh` 在 fork clone 的預設 repo 就是上游，裸跑 `gh pr create` 必然打上去。

```powershell
gh repo set-default SanHsien/SkillSpector   # 每個 clone 先跑一次
gh repo set-default --view                  # 必須回 SanHsien/SkillSpector
gh pr create --repo SanHsien/SkillSpector --base main --head <分支>
```

建完**讀輸出的 URL**，owner 必須是 `SanHsien`。不是就立刻 `gh pr close` 留言道歉說明開錯 repo，
再對 origin 重開，並主動告訴維護者。

**唯一例外：** 維護者在**這次對話**明確同意回貢上游。fork、建環境、開 PR、比照其他 repo、
合併回 main、「所有專案都照做」都不是例外。上游要求每筆 commit 附 DCO `Signed-off-by`
（`git commit -s`，見 `CONTRIBUTING.md`），所以回貢的門檻本來就更高：先確認是上游會接受的
bug 修正，而不是這條維護線獨有的 Windows 骨架或繁中文件。判準見 [`FORK.md`](../../FORK.md)。

也不要 push 到 `upstream` remote，不要 force-push `main`，不要刪 `upstream` remote。
